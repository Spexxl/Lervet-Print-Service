# === EXEMPLO DE CONFIGURAÇÃO COMPLETA ===
# Este arquivo demonstra todas as opções de configuração disponíveis

# === CONFIGURAÇÃO DO MODO DE CONEXÃO DA IMPRESSORA ===
# Opções válidas: 'usb' (porta COM), 'network' (TCP/IP) ou 'windows' (Windows Printer API)
PRINTER_MODE=network

# --- Configuração para modo USB (caso utilize USB/COM) ---
# Informe a porta COM correspondente (exemplo: COM3, COM4, etc.)
# Se não especificado, o sistema tentará todas as portas COM disponíveis
# PRINTER_PORT=COM3

# --- Configuração para modo de rede (caso utilize impressora Ethernet) ---
# Informe o IP da impressora de rede abaixo
PRINTER_IP=192.168.1.101
# Porta TCP da impressora de rede (9100 é padrão para Epson)
PRINTER_IP_PORT=9100

# --- Configuração para modo Windows (caso utilize Windows Printer API) ---
# NOVO: Suporte para impressoras instaladas no Windows que não aparecem como COM
# Informe o nome exato da impressora conforme aparece no Windows
# Para ver as impressoras disponíveis, execute: wmic printer get name
# Se não especificado, será feita busca automática por impressoras EPSON/TM-*
# PRINTER_NAME=EPSON TM-T20X Receipt

# === CONFIGURAÇÃO DO BACKEND ===
# URL do backend para receber tokens via WebSocket
BACKEND_URL=https://lervet-lervet-token-backend.lwzypg.easypanel.host

# === EXEMPLOS DE CONFIGURAÇÃO POR MODO ===

# Exemplo 1: Modo USB com porta específica
# PRINTER_MODE=usb
# PRINTER_PORT=COM3

# Exemplo 2: Modo USB com detecção automática
# PRINTER_MODE=usb
# (PRINTER_PORT não especificado - tentará todas as portas)

# Exemplo 3: Modo de rede
# PRINTER_MODE=network
# PRINTER_IP=192.168.1.100
# PRINTER_IP_PORT=9100

# Exemplo 4: Modo Windows com impressora específica
# PRINTER_MODE=windows
# PRINTER_NAME=EPSON TM-T20X Receipt

# Exemplo 5: Modo Windows com detecção automática
# PRINTER_MODE=windows
# (PRINTER_NAME não especificado - buscará EPSON/TM- automaticamente)